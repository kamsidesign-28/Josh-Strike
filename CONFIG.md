# ⚙️ JOSH-STRIKE Configuration Guide

The config file is auto-generated at `~/.josh_config.json` on first run.

---

## 📋 Full Configuration Options

```json
{
  "sim_users": 10,
  "sim_delay_min": 1.0,
  "sim_delay_max": 3.0,
  "parallel_threads": 5,
  "auto_retry": true,
  "retry_count": 3,
  "report_variants": 5,
  "max_retry_delay": 10,
  "timeout_seconds": 30,
  "log_level": "INFO",
  "save_reports": true,
  "export_format": "json"
}
